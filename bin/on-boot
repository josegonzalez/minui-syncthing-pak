#!/bin/sh
bindir="$(dirname "$0")"

main() {
    "$bindir/service-on" &
}

if [ -f "$LOGS_PATH/Syncthing.txt" ]; then
    mv "$LOGS_PATH/Syncthing.txt" "$LOGS_PATH/Syncthing.txt.old"
fi

main "$@" >"$LOGS_PATH/Syncthing.txt" 2>&1
